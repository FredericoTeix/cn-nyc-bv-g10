name: Trip tests
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Start containers
        run: docker-compose -f "trip/docker-compose.yml" up

      - name: Stop containers
        run: docker-compose -f "trip/docker-compose.yml" down
